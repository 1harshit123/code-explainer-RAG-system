[
  {
    "id": "backend/reademail.py::init_gmail_service",
    "name": "init_gmail_service",
    "type": "function",
    "parent_class": null,
    "filepath": "backend/reademail.py",
    "start_line": 9,
    "end_line": 10,
    "signature": "def init_gmail_service(client_file, api_name='gmail', api_version='v1', scopes=['https://mail.google.com/']):",
    "docstring": "",
    "decorators": [],
    "calls": [
      "create_service"
    ],
    "source": "def init_gmail_service(client_file, api_name='gmail', api_version='v1', scopes=['https://mail.google.com/']):\n    return create_service(client_file, api_name, api_version, scopes)",
    "embed_text": "File: backend/reademail.py\nName: init_gmail_service\nSignature: def init_gmail_service(client_file, api_name='gmail', api_version='v1', scopes=['https://mail.google.com/']):\nCalls: create_service\n\nSource code:\ndef init_gmail_service(client_file, api_name='gmail', api_version='v1', scopes=['https://mail.google.com/']):\n    return create_service(client_file, api_name, api_version, scopes)"
  },
  {
    "id": "backend/reademail.py::_extract_body",
    "name": "_extract_body",
    "type": "function",
    "parent_class": null,
    "filepath": "backend/reademail.py",
    "start_line": 12,
    "end_line": 26,
    "signature": "def _extract_body(payload):",
    "docstring": "",
    "decorators": [],
    "calls": [
      "base64.urlsafe_b64decode",
      "base64.urlsafe_b64decode(part['body']['data']).decode",
      "base64.urlsafe_b64decode(payload['body']['data']).decode",
      "base64.urlsafe_b64decode(subpart['body']['data']).decode"
    ],
    "source": "def _extract_body(payload):\n    body = '<Text body not available>'\n    if 'parts' in payload:\n        for part in payload['parts']:\n            if part['mimeType'] == 'multipart/alternative':\n                for subpart in part['parts']:\n                    if subpart['mimeType'] == 'text/plain' and 'data' in subpart['body']:\n                        body = base64.urlsafe_b64decode(subpart['body']['data']).decode('utf-8')\n                        break\n            elif part['mimeType'] == 'text/plain' and 'data' in part['body']:\n                body = base64.urlsafe_b64decode(part['body']['data']).decode('utf-8')\n                break\n    elif 'body' in payload and 'data' in payload['body']:\n        body = base64.urlsafe_b64decode(payload['body']['data']).decode('utf-8')\n    return body",
    "embed_text": "File: backend/reademail.py\nName: _extract_body\nSignature: def _extract_body(payload):\nCalls: base64.urlsafe_b64decode, base64.urlsafe_b64decode(part['body']['data']).decode, base64.urlsafe_b64decode(payload['body']['data']).decode, base64.urlsafe_b64decode(subpart['body']['data']).decode\n\nSource code:\ndef _extract_body(payload):\n    body = '<Text body not available>'\n    if 'parts' in payload:\n        for part in payload['parts']:\n            if part['mimeType'] == 'multipart/alternative':\n                for subpart in part['parts']:\n                    if subpart['mimeType'] == 'text/plain' and 'data' in subpart['body']:\n                        body = base64.urlsafe_b64decode(subpart['body']['data']).decode('utf-8')\n                        break\n            elif part['mimeType'] == 'text/plain' and 'data' in part['body']:\n                body = base64.urlsafe_b64decode(part['body']['data']).decode('utf-8')\n                break\n    elif 'body' in payload and 'data' in payload['body']:\n        body = base64.urlsafe_b64decode(payload['body']['data']).decode('utf-8')\n    return body"
  },
  {
    "id": "backend/reademail.py::get_email_messages",
    "name": "get_email_messages",
    "type": "function",
    "parent_class": null,
    "filepath": "backend/reademail.py",
    "start_line": 28,
    "end_line": 58,
    "signature": "def get_email_messages(service, user_id='me', label_ids=None, folder_name='INBOX', max_results=5):",
    "docstring": "",
    "decorators": [],
    "calls": [
      "ValueError",
      "folder_name.lower",
      "label['name'].lower",
      "label_ids.append",
      "label_results.get",
      "len",
      "messages.extend",
      "min",
      "next",
      "result.get",
      "service.users",
      "service.users().labels",
      "service.users().labels().list",
      "service.users().labels().list(userId=user_id).execute",
      "service.users().messages",
      "service.users().messages().list"
    ],
    "source": "def get_email_messages(service, user_id='me', label_ids=None, folder_name='INBOX', max_results=5):\n    messages = []\n    next_page_token = None\n\n    if folder_name:\n        label_results = service.users().labels().list(userId=user_id).execute()\n        labels = label_results.get('labels', [])\n        folder_label_id = next((label['id'] for label in labels if label['name'].lower() == folder_name.lower()), None)\n        if folder_label_id:\n            if label_ids:\n                label_ids.append(folder_label_id)\n            else:\n                label_ids = [folder_label_id]\n        else:\n            raise ValueError(f\"Folder '{folder_name}' not found.\")\n\n    while True:\n        result = service.users().messages().list(\n            userId=user_id,\n            labelIds=label_ids,\n            maxResults=min(500, max_results - len(messages)) if max_results else 500,\n            pageToken=next_page_token\n        ).execute()\n\n        messages.extend(result.get('messages', []))\n        next_page_token = result.get('nextPageToken')\n\n        if not next_page_token or (max_results and len(messages) >= max_results):\n            break\n\n    return messages[:max_results] if max_results else messages",
    "embed_text": "File: backend/reademail.py\nName: get_email_messages\nSignature: def get_email_messages(service, user_id='me', label_ids=None, folder_name='INBOX', max_results=5):\nCalls: ValueError, folder_name.lower, label['name'].lower, label_ids.append, label_results.get, len, messages.extend, min, next, result.get, service.users, service.users().labels, service.users().labels().list, service.users().labels().list(userId=user_id).execute, service.users().messages\n\nSource code:\ndef get_email_messages(service, user_id='me', label_ids=None, folder_name='INBOX', max_results=5):\n    messages = []\n    next_page_token = None\n\n    if folder_name:\n        label_results = service.users().labels().list(userId=user_id).execute()\n        labels = label_results.get('labels', [])\n        folder_label_id = next((label['id'] for label in labels if label['name'].lower() == folder_name.lower()), None)\n        if folder_label_id:\n            if label_ids:\n                label_ids.append(folder_label_id)\n            else:\n                label_ids = [folder_label_id]\n        else:\n            raise ValueError(f\"Folder '{folder_name}' not found.\")\n\n    while True:\n        result = service.users().messages().list(\n            userId=user_id,\n            labelIds=label_ids,\n            maxResults=min(500, max_results - len(messages)) if max_results else 500,\n            pageToken=next_page_token\n        ).execute()\n\n        messages.extend(result.get('messages', []))\n        next_page_token = result.get('nextPageToken')\n\n        if not next_page_token or (max_results and len(messages) >= max_results):\n            break\n\n    return messages[:max_results] if max_results else messages"
  },
  {
    "id": "backend/reademail.py::get_email_message_details",
    "name": "get_email_message_details",
    "type": "function",
    "parent_class": null,
    "filepath": "backend/reademail.py",
    "start_line": 60,
    "end_line": 89,
    "signature": "def get_email_message_details(service, msg_id):",
    "docstring": "",
    "decorators": [],
    "calls": [
      "', '.join",
      "_extract_body",
      "any",
      "header['name'].lower",
      "message.get",
      "message.get('labelIds', []).count",
      "next",
      "part.get",
      "payload.get",
      "service.users",
      "service.users().messages",
      "service.users().messages().get"
    ],
    "source": "def get_email_message_details(service, msg_id):\n    message = service.users().messages().get(userId='me', id=msg_id, format='full').execute()\n    payload = message['payload']\n    headers = payload.get('headers', [])\n\n    subject = next((header['value'] for header in headers if header['name'].lower() == 'subject'), None)\n    if not subject:\n        subject = message.get('subject', 'No subject')\n\n    sender = next((header['value'] for header in headers if header['name'] == 'From'), 'No sender')\n    recipients = next((header['value'] for header in headers if header['name'] == 'To'), 'No recipients')\n    snippet = message.get('snippet', 'No snippet')\n    has_attachments = any(part.get('filename') for part in payload.get('parts', []) if part.get('filename'))\n    date = next((header['value'] for header in headers if header['name'] == 'Date'), 'No date')\n    star = message.get('labelIds', []).count('STARRED') > 0\n    label = ', '.join(message.get('labelIds', []))\n\n    body = _extract_body(payload)\n\n    return {\n        'subject': subject,\n        'sender': sender,\n        'recipients': recipients,\n        'body': body,\n        'snippet': snippet,\n        'has_attachments': has_attachments,\n        'date': date,\n        'star': star,\n        'label': label,\n    }",
    "embed_text": "File: backend/reademail.py\nName: get_email_message_details\nSignature: def get_email_message_details(service, msg_id):\nCalls: ', '.join, _extract_body, any, header['name'].lower, message.get, message.get('labelIds', []).count, next, part.get, payload.get, service.users, service.users().messages, service.users().messages().get\n\nSource code:\ndef get_email_message_details(service, msg_id):\n    message = service.users().messages().get(userId='me', id=msg_id, format='full').execute()\n    payload = message['payload']\n    headers = payload.get('headers', [])\n\n    subject = next((header['value'] for header in headers if header['name'].lower() == 'subject'), None)\n    if not subject:\n        subject = message.get('subject', 'No subject')\n\n    sender = next((header['value'] for header in headers if header['name'] == 'From'), 'No sender')\n    recipients = next((header['value'] for header in headers if header['name'] == 'To'), 'No recipients')\n    snippet = message.get('snippet', 'No snippet')\n    has_attachments = any(part.get('filename') for part in payload.get('parts', []) if part.get('filename'))\n    date = next((header['value'] for header in headers if header['name'] == 'Date'), 'No date')\n    star = message.get('labelIds', []).count('STARRED') > 0\n    label = ', '.join(message.get('labelIds', []))\n\n    body = _extract_body(payload)\n\n    return {\n        'subject': subject,\n        'sender': sender,\n        'recipients': recipients,\n        'body': body,\n        'snippet': snippet,\n        'has_attachments': has_attachments,\n        'date': date,\n        'star': star,\n        'label': label,\n    }"
  },
  {
    "id": "backend/reademail.py::main",
    "name": "main",
    "type": "function",
    "parent_class": null,
    "filepath": "backend/reademail.py",
    "start_line": 92,
    "end_line": 117,
    "signature": "def main():",
    "docstring": "",
    "decorators": [],
    "calls": [
      "enumerate",
      "get_email_message_details",
      "get_email_messages",
      "init_gmail_service",
      "len",
      "print"
    ],
    "source": "def main():\n        # Initialize the Gmail API service\n        service = init_gmail_service(\"credentials.json\")  # Make sure your credentials file path is correct\n\n        # Get recent 5 email messages\n        messages = get_email_messages(service, max_results=5)\n\n        # Loop through and print details of each email\n        for idx, msg in enumerate(messages, 1):\n            msg_id = msg['id']\n            details = get_email_message_details(service, msg_id)\n            \n            print(\"=\" * 80)\n            print(f\"Email #{idx}\")\n            print(f\"Subject       : {details['subject']}\")\n            print(f\"From          : {details['sender']}\")\n            print(f\"To            : {details['recipients']}\")\n            print(f\"Date          : {details['date']}\")\n            print(f\"Labels        : {details['label']}\")\n            print(f\"Starred       : {'Yes' if details['star'] else 'No'}\")\n            print(f\"Attachments   : {'Yes' if details['has_attachments'] else 'No'}\")\n            print(f\"Snippet       : {details['snippet']}\")\n            print(\"\\nBody Preview:\")\n            print(details['body'][:500] + ('...' if len(details['body']) > 500 else ''))  # Print first 500 chars\n            print(\"=\" * 80)\n            print(\"\\n\")",
    "embed_text": "File: backend/reademail.py\nName: main\nSignature: def main():\nCalls: enumerate, get_email_message_details, get_email_messages, init_gmail_service, len, print\n\nSource code:\ndef main():\n        # Initialize the Gmail API service\n        service = init_gmail_service(\"credentials.json\")  # Make sure your credentials file path is correct\n\n        # Get recent 5 email messages\n        messages = get_email_messages(service, max_results=5)\n\n        # Loop through and print details of each email\n        for idx, msg in enumerate(messages, 1):\n            msg_id = msg['id']\n            details = get_email_message_details(service, msg_id)\n            \n            print(\"=\" * 80)\n            print(f\"Email #{idx}\")\n            print(f\"Subject       : {details['subject']}\")\n            print(f\"From          : {details['sender']}\")\n            print(f\"To            : {details['recipients']}\")\n            print(f\"Date          : {details['date']}\")\n            print(f\"Labels        : {details['label']}\")\n            print(f\"Starred       : {'Yes' if details['star'] else 'No'}\")\n            print(f\"Attachments   : {'Yes' if details['has_attachments'] else 'No'}\")\n            print(f\"Snippet       : {details['snippet']}\")\n            print(\"\\nBody Preview:\")\n            print(details['body'][:500] + ('...' if len(details['body']) > 500 else ''))  # Print first 500 chars\n            print(\"=\" * 80)\n            print(\"\\n\")"
  },
  {
    "id": "backend/geminiautomation.py::generate",
    "name": "generate",
    "type": "function",
    "parent_class": null,
    "filepath": "backend/geminiautomation.py",
    "start_line": 16,
    "end_line": 24,
    "signature": "def generate():",
    "docstring": "",
    "decorators": [],
    "calls": [
      "client.models.generate_content",
      "genai.Client",
      "print"
    ],
    "source": "def generate():\n    client = genai.Client(\n        api_key=api_key,\n    )\n\n    response = client.models.generate_content(\n    model='gemini-2.0-flash-001', contents='Why is the sky blue?'\n    )\n    print(response.text)",
    "embed_text": "File: backend/geminiautomation.py\nName: generate\nSignature: def generate():\nCalls: client.models.generate_content, genai.Client, print\n\nSource code:\ndef generate():\n    client = genai.Client(\n        api_key=api_key,\n    )\n\n    response = client.models.generate_content(\n    model='gemini-2.0-flash-001', contents='Why is the sky blue?'\n    )\n    print(response.text)"
  },
  {
    "id": "backend/gmailapi.py::create_service",
    "name": "create_service",
    "type": "function",
    "parent_class": null,
    "filepath": "backend/gmailapi.py",
    "start_line": 7,
    "end_line": 50,
    "signature": "def create_service(client_secret_file, api_name, api_version, *scopes, prefix=''):",
    "docstring": "",
    "decorators": [],
    "calls": [
      "Credentials.from_authorized_user_file",
      "InstalledAppFlow.from_client_secrets_file",
      "Request",
      "build",
      "creds.refresh",
      "creds.to_json",
      "flow.run_local_server",
      "open",
      "os.getcwd",
      "os.mkdir",
      "os.path.exists",
      "os.path.join",
      "os.remove",
      "print",
      "token.write"
    ],
    "source": "def create_service(client_secret_file, api_name, api_version, *scopes, prefix=''):\n    CLIENT_SECRET_FILE = client_secret_file\n    API_SERVICE_NAME = api_name\n    API_VERSION = api_version\n    SCOPES = [scope for scope in scopes[0]]\n\n    creds = None\n    working_dir = os.getcwd()\n    token_dir = 'token files'\n    token_file = f'token_{API_SERVICE_NAME}_{API_VERSION}{prefix}.json'\n    token_path = os.path.join(working_dir, token_dir)\n    token_file_path = os.path.join(token_path, token_file)\n\n    # Make sure token dir exists\n    if not os.path.exists(token_path):\n        os.mkdir(token_path)\n\n    # Load existing credentials if available\n    if os.path.exists(token_file_path):\n        creds = Credentials.from_authorized_user_file(token_file_path, SCOPES)\n\n    # If no valid creds, refresh or login\n    if not creds or not creds.valid:\n        if creds and creds.expired and creds.refresh_token:\n            try:\n                creds.refresh(Request())\n            except Exception as e:\n                print(\"Token refresh failed:\", e)\n                creds = None\n        if not creds:\n            flow = InstalledAppFlow.from_client_secrets_file(CLIENT_SECRET_FILE, SCOPES)\n            creds = flow.run_local_server(port=0)\n        with open(token_file_path, 'w') as token:\n            token.write(creds.to_json())\n\n    try:\n        service = build(API_SERVICE_NAME, API_VERSION, credentials=creds, static_discovery=False)\n        print(f\"{API_SERVICE_NAME} {API_VERSION} service created successfully\")\n        return service\n    except Exception as e:\n        print(\"Error creating service:\", e)\n        if os.path.exists(token_file_path):\n            os.remove(token_file_path)\n        return None",
    "embed_text": "File: backend/gmailapi.py\nName: create_service\nSignature: def create_service(client_secret_file, api_name, api_version, *scopes, prefix=''):\nCalls: Credentials.from_authorized_user_file, InstalledAppFlow.from_client_secrets_file, Request, build, creds.refresh, creds.to_json, flow.run_local_server, open, os.getcwd, os.mkdir, os.path.exists, os.path.join, os.remove, print, token.write\n\nSource code:\ndef create_service(client_secret_file, api_name, api_version, *scopes, prefix=''):\n    CLIENT_SECRET_FILE = client_secret_file\n    API_SERVICE_NAME = api_name\n    API_VERSION = api_version\n    SCOPES = [scope for scope in scopes[0]]\n\n    creds = None\n    working_dir = os.getcwd()\n    token_dir = 'token files'\n    token_file = f'token_{API_SERVICE_NAME}_{API_VERSION}{prefix}.json'\n    token_path = os.path.join(working_dir, token_dir)\n    token_file_path = os.path.join(token_path, token_file)\n\n    # Make sure token dir exists\n    if not os.path.exists(token_path):\n        os.mkdir(token_path)\n\n    # Load existing credentials if available\n    if os.path.exists(token_file_path):\n        creds = Credentials.from_authorized_user_file(token_file_path, SCOPES)\n\n    # If no valid creds, refresh or login\n    if not creds or not creds.valid:\n        if creds and creds.expired and creds.refresh_token:\n            try:\n                creds.refresh(Request())\n            except Exception as e:\n                print(\"Token refresh failed:\", e)\n                creds = None\n        if not creds:\n            flow = InstalledAppFlow.from_client_secrets_file(CLIENT_SECRET_FILE, SCOPES)\n            creds = flow.run_local_server(port=0)\n        with open(token_file_path, 'w') as token:\n            token.write(creds.to_json())\n\n    try:\n        service = build(API_SERVICE_NAME, API_VERSION, credentials=creds, static_discovery=False)\n        print(f\"{API_SERVICE_NAME} {API_VERSION} service created successfully\")\n        return service\n    except Exception as e:\n        print(\"Error creating service:\", e)\n        if os.path.exists(token_file_path):\n            os.remove(token_file_path)\n        return None"
  }
]